# Server Configuration  
PORT=3000  
  
# Optional: CORS origins (comma-separated, leave empty for all origins)  
# CORS_ORIGINS=http://localhost:3000,https://example.com  
  
# Optional: Enable request logging  
# LOG_REQUESTS=true 
